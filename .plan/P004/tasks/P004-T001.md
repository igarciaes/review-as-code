# P004-T001 — Add mandatory post-operation record verification to SKILL.md

**Status:** Verified
**Owner:** Implementer
**Plan:** P004

## Objective

Add a `## Post-operation verification` section to `SKILL.md` that the skill runs after every operation execution, and reference it from the `## Operations` intro so the current "confirm the record stays SPEC-conformant" wording becomes a mandatory, named step.

**Depends on:**

- None

## Definition of Done

- [x] `SKILL.md` contains a `## Post-operation verification` section placed after `## Operations`.
- [x] The section requires verifying the records after every operation execution before reporting completion, and requires correcting any deviation found.
- [x] The section lists concrete checks: every new lifecycle log entry records `id`, `value`, `date`, `actor`, and `content` (SPEC §10); entry actors match the operation's role boundaries (SPEC §9.3–9.5); the derived `State` matches the finding record and lifecycle logs (SPEC §8.5, §20.10); no historical log entry was rewritten; reviewer-owned sections were not silently modified; only the intended files changed; exactly one operation was performed (SPEC §18.6); `Close` runs only when no finding is outstanding (SPEC §18.5); schema validation runs when applicable (SPEC §16.1); and `Inspect` modifies nothing.
- [x] The `## Operations` intro references the new section as a mandatory step.
- [x] `SKILL.md` remains aligned with `SPEC.md` and conformant with the Agent Skills spec.

## Implementation

Added a `## Post-operation verification` section to `SKILL.md` immediately after `## Operations`, listing the concrete checks from the Definition of Done and requiring deviation correction before reporting completion. Updated the `## Operations` intro to reference the mandatory section. `SKILL.md` remains aligned with `SPEC.md` and conformant with the Agent Skills spec (valid `name`, `description`, `license`, `metadata` frontmatter preserved).

## Implementation Evidence

- Commit: (none — no commit in this plan)
- Test: (manual review; no test suite per `AGENTS.md`)
- Files:
  - `SKILL.md`: new `## Post-operation verification` section and updated `## Operations` intro

## Verification

**Result:** Verified

**By:** planner-agent
**Date:** 2026-09-15