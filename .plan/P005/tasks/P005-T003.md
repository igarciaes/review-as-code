# P005-T003 — Add F008 example (accepted_risk → implemented → verified)

**Status:** Verified
**Owner:** Implementer
**Plan:** P005

## Objective

Add `examples/R042/findings/F008.md` demonstrating the Reviewer-`accepted_risk` decision followed by
Author implementation and Reviewer verification.

**Depends on:**

- P005-T001

## Definition of Done

- [x] `examples/R042/findings/F008.md` exists with `State: decision=accepted_risk, implementation=implemented, verification=verified`.
- [x] `D001 — accepted_risk` records `Actor: Reviewer`; `I001 — implemented` records `Actor: Author` with evidence; `V001 — verified` records `Actor: Reviewer` with evidence.
- [x] The finding validates against `schemas/finding.schema.json`.
- [x] `review.md` for R042 is not modified (filesystem is the index).

## Implementation

Added `examples/R042/findings/F008.md` with `State: decision=accepted_risk, implementation=implemented, verification=verified`. `D001 — accepted_risk` records `Actor: Reviewer`; `I001 — implemented` records `Actor: Author` with evidence (commit `abcd12`, `src/payment/tokens.ts`); `V001 — verified` records `Actor: Reviewer` with evidence (commit, check, result). `review.md` for R042 was not modified. Projected via `SPEC.md` §16.1 and validated against `schemas/finding.schema.json` (jsonschema, 0 errors).

## Implementation Evidence

- Commit: (none — no commit in this plan)
- Test: (manual validation against `schemas/finding.schema.json`)
- Files:
  - `examples/R042/findings/F008.md`

## Verification

**Result:** Verified

**By:** planner-agent
**Date:** 2026-09-15