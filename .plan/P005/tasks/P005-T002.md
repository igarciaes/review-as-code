# P005-T002 — Enforce the precondition in SKILL.md

**Status:** Implemented
**Owner:** Implementer
**Plan:** P005

## Objective

Enforce the eligible-decision precondition in `SKILL.md` so an agent refuses to implement a finding
that has no `accepted` (Author) or `accepted_risk` (Reviewer) decision.

**Depends on:**

- P005-T001

## Definition of Done

- [ ] `SKILL.md` Operations → Implement records the precondition: do not implement without an eligible Decision Log entry.
- [ ] `SKILL.md` Post-operation verification checks that implementation followed an eligible decision.
- [ ] `SKILL.md` remains role-agnostic and conformant with the Agent Skills spec, consistent with `SPEC.md` §9.3.

## Implementation

Enforced the eligible-decision precondition in `SKILL.md`: the Operations → Implement bullet refuses to implement a finding whose Decision Log lacks `accepted` (Author) or `accepted_risk` (Reviewer), referencing `SPEC.md` §8.1, §8.3, §18.2; the Post-operation verification adds a check that implementation followed an eligible decision; the Ownership section notes the Reviewer `accepted_risk` exception consistent with `SPEC.md` §9.3; the derived-State check reference was updated from `§20.10` to `§20.11` for the renumbered conformance list.

## Implementation Evidence

- Commit: (none — no commit in this plan)
- Test: (manual review; no test suite per `AGENTS.md`)
- Files:
  - `SKILL.md`

## Verification

(empty — not verified)