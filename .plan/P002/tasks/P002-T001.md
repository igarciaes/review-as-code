# P002-T001 — Rewrite SKILL.md as a role-agnostic operation skill

**Status:** Draft
**Owner:** Implementer
**Plan:** P002

## Objective

Restructure `SKILL.md` so it executes only the operations required by `SPEC.md` and enforces spec conformance, without defining or assuming a specific role for the calling agent.

**Depends on:**

- None

## Definition of Done

- [ ] `SKILL.md` no longer assigns a role to the calling agent (the `## Roles` section is reframed; no identity assignment).
- [ ] Ownership boundaries from `SPEC.md` §9 (reviewer-owned Observation/Recommendation/Severity/Location, Author-authored Implementation Log, Reviewer-authored Verification Log, shared Decision Log) are presented as enforced constraints, not identities.
- [ ] The skill states explicitly that it assigns no role and that the caller's role comes from context/policy.
- [ ] The skill supports only the SPEC-defined operations: Review, Implement, Decide, Verify, Inspect, Close.
- [ ] Each operation states its input, output, and SPEC-conformance checks (append-only log entry with stable ID, actor, date, content; derived `State` update; minimum write scope; no silent rewrite).
- [ ] Discovery, Findings, Lifecycle logs, Outstanding Findings, and Final Invariant sections remain consistent with `SPEC.md`.
- [ ] `SKILL.md` remains conformant with the Agent Skills spec (valid `name`, `description`, `license`, `metadata` frontmatter).

## Implementation

Reframe the existing role-assignment framing into role-agnostic operation guidance while preserving the append-only logs, derived `State`, closure, and outstanding-findings rules.

## Implementation Evidence

- Commit: (none — no commit in this plan)
- Test: (manual review; no test suite per `AGENTS.md`)

## Verification

**Result:** Pending

**By:** planner-agent
**Date:** YYYY-MM-DD
