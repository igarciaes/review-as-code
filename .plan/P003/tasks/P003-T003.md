# P003-T003 — Add CHANGELOG entry

**Status:** Verified
**Owner:** Implementer
**Plan:** P003

## Objective

Add a Keep a Changelog `## [Unreleased]` entry documenting the one-operation-per-round rule and the actor conformance/schema tightening.

**Depends on:**

- P003-T001
- P003-T002

## Definition of Done

- [x] `CHANGELOG.md` has an `## [Unreleased]` section at the top.
- [x] A `### Changed` entry records the one-operation-per-round rule (`SPEC.md` §18.6, `SKILL.md`).
- [x] A `### Fixed` entry records the actor conformance tightening (`SPEC.md` §20 item 9), the `content`-required schema change, and the `AGENTS.md` checklist addition.

## Implementation

Added a Keep a Changelog `## [Unreleased]` section at the top of `CHANGELOG.md` with a `### Changed` entry recording the one-operation-per-round rule (`SPEC.md` §18.6, `SKILL.md`) and a `### Fixed` entry recording the actor conformance tightening (`SPEC.md` §20 item 9), the `content`-required schema change, and the `AGENTS.md` checklist addition.

## Implementation Evidence

- Commit: (none — no commit in this plan)
- Test: (manual review; no test suite per `AGENTS.md`)
- Files:
  - `CHANGELOG.md`: `## [Unreleased]` section with `### Changed` and `### Fixed` entries

## Verification

**Result:** Verified

**By:** planner-agent
**Date:** 2026-09-15