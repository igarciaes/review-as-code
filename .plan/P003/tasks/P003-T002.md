# P003-T002 — Tighten actor conformance and schema alignment

**Status:** Verified
**Owner:** Implementer
**Plan:** P003

## Objective

Make the actor requirement explicit and validated: tighten `SPEC.md` §20 conformance item 9 so it names the actor, require `content` on `schemas/finding.schema.json` `logEntry` to match the mandatory entry fields of `SPEC.md` §10, and add an actor check to the `AGENTS.md` validation checklist.

**Depends on:**

- None

## Definition of Done

- [x] `SPEC.md` §20 conformance item 9 requires each lifecycle log entry to record its actor.
- [x] `schemas/finding.schema.json` `logEntry` `required` array includes `content` alongside `id`, `value`, `date`, and `actor`.
- [x] `AGENTS.md` "Building and validating" checklist includes checking that every lifecycle log entry records an actor consistent with `SPEC.md` §9.3–9.5.
- [x] Existing examples still validate against the updated finding schema (every log entry has actor and content; `Pending.` placeholders represent empty logs with no entries).

## Implementation

Tightened `SPEC.md` §20 conformance item 9 so it requires each lifecycle log entry to record its actor. Added `content` to the `logEntry` `required` array in `schemas/finding.schema.json` alongside `id`, `value`, `date`, and `actor`, matching the mandatory entry fields of `SPEC.md` §10. Added a check to the `AGENTS.md` "Building and validating" checklist that every lifecycle log entry records an actor consistent with `SPEC.md` §9.3–9.5. Existing examples remain valid: every log entry carries both an actor and content, and `Pending.` placeholders represent empty logs with no entries.

## Implementation Evidence

- Commit: (none — no commit in this plan)
- Test: (manual review; no test suite per `AGENTS.md`)
- Files:
  - `SPEC.md`: §20 conformance item 9 now names the actor requirement
  - `schemas/finding.schema.json`: `logEntry` `required` now includes `content`
  - `AGENTS.md`: added actor check to the "Building and validating" checklist

## Verification

**Result:** Verified

**By:** planner-agent
**Date:** 2026-09-15