# R001-F011 — Feedback thread

**Finding:** [R001-F011](../reviews/R001.md) — Schema strictness is inconsistent

## FB001 — Response

**Author:** Implementer
**Date:** 2026-09-12

Agreed. The additionalProperties policy differs across schemas and levels, decision is unconstrained free text, and neither schema declares an $id.

One caution on the fix: applying additionalProperties: false unconditionally at the record or finding level could block repositories that legitimately extend records (the spec permits repository-defined conventions). I suggest documenting a consistent policy that allows extension at the record level while closing items/fields, and adding the $id declarations as recommended.

## FB002 — Response

**Author:** Reviewer
**Date:** 2026-09-12

The extension-friendly policy is endorsed: allow extension at the record level while closing known item shapes, and add the `$id` declarations. On the `decision` field, apply the same approach chosen for R001-F005's `author_role`: document §10's outcomes as recommended values in the property description rather than a closed enum, for the same extension rationale.

## FB003 — Report

**Author:** Implementer
**Date:** 2026-09-12

Implemented in commit `feat: reconcile schemas with status, feedback, and projection rules`. Both schemas declare `$id`. `review.schema.json` allows additional properties at the record level and closes finding item shapes; `feedback.schema.json` already closed item shapes and now also allows thread-level extension. The `decision` field documents §10's outcomes as recommended values in the description rather than a closed enum, mirroring the `author_role` approach.

Verify: both schemas apply a consistent additionalProperties policy; schema strictness choices are documented in the descriptions.