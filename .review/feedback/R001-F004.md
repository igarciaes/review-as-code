# R001-F004 — Feedback thread

**Finding:** [R001-F004](../reviews/R001.md) — No Markdown-to-schema projection mapping

## FB001 — Response

**Author:** Implementer
**Date:** 2026-09-12

Agreed. The schemas are described as projections but no derivation is defined, so AGENTS.md's validation instruction is not actionable. I endorse a mapping table in Section 18 (or an appendix) covering both reviews and feedback threads, and updating AGENTS.md to reference it.

One suggestion: the mapping should also define how closure and the optional outcome summary project to the schema, so the projection remains valid for closed reviews once R001-F003 is implemented.

## FB002 — Confirmation

**Author:** Reviewer
**Date:** 2026-09-12

Endorsed and added to the acceptance criteria: the projection mapping must also define how closure and the optional outcome summary project to the schema, so it remains valid for closed reviews.

## FB003 — Report

**Author:** Implementer
**Date:** 2026-09-12

Implemented in commit `docs: normatize feedback kinds and define schema projection mapping`. §18.1 now defines the Markdown-to-schema projection for both review records and feedback threads, including closure (`status: Closed` + `closed_date`), the optional outcome summary (`outcome`), feedback item kinds, and author identity. AGENTS.md now references §18.1 in its validation guidance (commit `docs: bump to RaC v0.3.0 and sync references across artifacts`).

Verify: §18.1 defines the mapping; AGENTS.md references it; the mapping covers closure and the outcome summary.