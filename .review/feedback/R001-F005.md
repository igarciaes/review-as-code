# R001-F005 — Feedback thread

**Finding:** [R001-F005](../reviews/R001.md) — Feedback item fields under-specified; schema role enum conflicts

## FB001 — Response

**Author:** Implementer
**Date:** 2026-09-12

Agreed. The kind convention appears only in examples, items carry no author identity, and the schema's Reviewer | Implementer | Verifier enum is stricter than Section 8.5's reviewer/implementer framing. I endorse normatizing the `## FB### — Kind` heading pattern with recommended kinds, adding an optional author identity, acknowledging the Verifier, and opening the role vocabulary for repository extension.

One question before I implement: the proposed "Report" kind does not appear in any example. Should the recommended kinds stay exactly Clarification, Response, and Confirmation, or is Report needed? Also please confirm author identity remains optional so existing threads are not invalidated.

## FB002 — Response

**Author:** Reviewer
**Date:** 2026-09-12

Keep Report. With R001-F001 resolved, the feedback thread is the implementer's reporting channel; progress and completion reports are neither questions (Clarification) nor confirmations (Confirmation), so the recommended kinds are Clarification, Response, Report, and Confirmation. And confirmed: the author identity field is optional, so existing threads remain valid. Both clarifications are recorded in the decision.

## FB003 — Report

**Author:** Implementer
**Date:** 2026-09-12

Implemented in commit `docs: normatize feedback kinds and define schema projection mapping` (SPEC §8.5) and `feat: reconcile schemas with status, feedback, and projection rules` (schema). §8.5 normatizes the `## FB### — Kind` heading with recommended kinds Clarification, Response, Report, and Confirmation, adds an optional author identity field, acknowledges the Verifier as a participant, and states repositories MAY extend the role vocabulary. `feedback.schema.json` opens `author_role` to a string with recommended values in the description and adds optional `kind` and `author` properties. This report item itself uses the Report kind.

Verify: §8.5 and `feedback.schema.json` agree on kinds, roles, and optional author identity; existing threads remain valid.